<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xdreamer.barangkuv3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".Session"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Barangku"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MasukActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DaftarActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".HomeProduk" />
        <activity android:name=".DetailKategori" />
        <activity android:name=".DetailProduk" />
        <activity android:name=".ViewItem" />
        <activity
            android:name=".ResetPassword"
            android:parentActivityName=".MasukActivity" />
        <activity android:name=".Transaksi"></activity>
    </application>

</manifest>